import { Card } from '@/components';

export function RedirectPageNotFound() {
  return (
    <Card className="flex flex-col items-center justify-center px-5 py-12 gap-6">
      <span>not found</span>
    </Card>
  );
}
